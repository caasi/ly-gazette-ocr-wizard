extends layout

block content
  p 您要提供的是
  a(href="https://#{image}", target="_blank")
    img(src="https://#{image}", style="max-height: 240px;")
  p 的文字內容，若確認無誤，請將文字貼在下面：
  form(method="POST")
    textarea#result(name="result", style="width: 960px; height: 240px")
    br
    input(type="submit", value="送出")
